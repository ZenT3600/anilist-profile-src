name: "Follower graph"
on:
  schedule:
  - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  graph:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo apt-get install -y gnuplot && bash ${GITHUB_WORKSPACE}/.github/followers.sh
