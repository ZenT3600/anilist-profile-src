name: "Follower graph"
on:
  schedule:
  - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  graph:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y gnuplot && bash .github/followers.sh
